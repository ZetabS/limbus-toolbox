export function chunkArray<T>(array: readonly T[], chunkSize: number) {
  let result: T[][] = [];
  for (let i = 0; i < array.length; i += chunkSize) {
    result.push(array.slice(i, i + chunkSize));
  }
  return result;
}
